type SAndLT = {
  activePlate: number;
  activeTime: number;
  animInProgress: boolean;
  animPortal: boolean;
};

const sAndLStates: SAndLT = {
  activePlate: 0,
  activeTime: 0,
  animInProgress: false,
  animPortal: false,
};

export default sAndLStates;
